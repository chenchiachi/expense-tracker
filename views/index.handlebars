<div class="col-lg-8 mx-auto">
  <div class="row my-3">
    <div class="col-3">
      <form action="/" method="GET" onchange="submit();">
        <select name="categoryId" class="form-select rounded-0 " id="categoryId" required>
          <option value="" {{#if (isEqual filteredCategory "" )}} selected {{/if}}>總支出</option>
          {{{ categoryHelper category filteredCategory }}}
        </select>
      </form>
    </div>
    <label for="totalAmount" class="col-3 text-end">總金額：</label>
    <div class="col-5" style="font-size: 50px;color:#e3885b">{{totalAmount}}
    </div>
  </div>
  <ul class="list-group">
    {{#each records}}
    <li class="list-group-item" style="color:dimgray">
      <div class="row">
        <div class="col-2 my-auto text-center">
          <i class="{{this.icon}} fa-2x" style="color:lightblue"></i>
        </div>
        <div class="col-4">
          <div class="fs-5">
            {{this.name}}
          </div>
          <div class="date">
            {{this.date}}
          </div>
        </div>
        <div class="col-2 my-auto text-end">
          {{this.amount}} 元
        </div>
        <div class="col-4 my-auto text-center">
          <a class="btn btn-light" href="/records/{{this._id}}/edit">修改</a>
          <form action="/records/{{this._id}}?_method=DELETE" method="POST" style="display: inline;"
            onsubmit="return confirm('是否確定刪除 {{this.name}} ?');">
            <button class="btn btn-light" type="submit">刪除</button>
          </form>
        </div>
      </div>
    </li>
    {{/each}}
  </ul>
</div>
<div class="d-flex justify-content-center m-5">
  <a class="btn btn-major px-5" href="/records/new">新增支出</a>
</div>